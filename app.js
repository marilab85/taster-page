let username = prompt("Are you in distress?");

if (username == "Yes") {
  alert("Hit the CALL button!");
  document.body.style.fontFamily = "monospace";
} else {
  alert("Just breathe. Might be a panic attack ");
}
